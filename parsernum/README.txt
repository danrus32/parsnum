Русский
Привет этот скрипт парсит дааные с сайта 999.md он парсит номера местоположение и никнэимы если вам нужен и  ид постабиспользуйте pars+id все даные записываютца в numbers.txt и data.db  для просмотра даных есть скрипт db_view.py а для поиска search_db.py так же при парсинге используетца прокси, даные парсятца анонимно
 Для парсинга запустите numpas.py в numpas.py  ид постов поставлен от 60000000 до 70000000 на этих ид больше всего актуальных постов а если хотие изменить то в конце скрипта есть #post id  в () меняите значение и все.
Нужно:
pip install re 
pip install sqlite3
pip install bs4
pip install lxml

English
Hi, this script parses data from the site 999.md, it parses location numbers and nicknames if you need and id post use pars+id all the data written in numbers.txt and data.db to view the data there is a script db_view.py and to search search_db.py also when parsing uses proxies, the data of the parser is anonymous
 For parsing, run numpas.py in numpas.py posts ID is set from 60,000,000 to 70,000,000 for these IDs are the most relevant posts, and if you want to change then there is a #post id in () at the end of the script, change the value and that's it.
Necessary:
pip install re
pip install sqlite3
pip install bs4
pip install lxml

Romana

Buna, acest script analizeaza datele de pe site-ul 999.md, analizeaza numerele si pseudonimele locatiei daca aveti nevoie li id-ul foloseste pars + id toate datele scrise cu numere.txt si data.db pentru a vizualiza datele exista un script db_view.py si pentru a cauta search_db.py de asemenea, atunci cand analizarea foloseste proxies, datele analizatorului sunt anonime
 Pentru analiza, executati numpas.py in posturile numpas.py ID-ul este setat de la 60.000.000 la 70.000.000 pentru aceste ID-uri sunt postarile cele mai relevante, iar daca dori?i sa schimbati, atunci exista un ID #post in () la sfarsitul scriptului, schimba?i valoarea si asta este.
Necesar:
pip instala re
pip instala sqlite3
pip instala bs4
pip instala lxml